if [ -z "$prefix" ]; then
    set prefix=($root)/boot/grub
fi
if loadfont $prefix/dejavu-bold-16.pf2 ; then
    set gfxmode=auto
    insmod all_video
    insmod gfxterm
    terminal_output gfxterm
fi
set theme=$prefix/live-theme/theme.txt
export theme
