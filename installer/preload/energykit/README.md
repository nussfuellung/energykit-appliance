# EnergyKit local HAOS app preload

This directory is copied into the HAOS `hassos-data` partition under
`supervisor/apps/local/energykit` by the EnergyKit installer ISO.

The release workflow renders `__IMAGE_BASE__` and `__REPOSITORY_URL__` before the ISO is built.
